<div class=side_header>
  <div >
   <%= link_to current_user.name, edit_user_path(current_user),class: "user_name" %>
  </div>
  <%= link_to "チャットを作成する", new_room_path ,class:"new-chat" %>
</div>
<div class=rooms>
  <div class="room_lists">
    <% current_user.rooms.each do |room| %>
    
     <%= link_to room.name, room_messages_path(room) ,class:"room_lists" %>
    <% end %>
    
  </div>
</div>
